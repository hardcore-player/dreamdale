using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Deal.Env
{
    public class SpaceCost : MonoBehaviour
    {
        // 解锁任务ID
        public int UnlockTask = -1;

        public List<Data_GameAsset> unlockAssets = new List<Data_GameAsset>();
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

